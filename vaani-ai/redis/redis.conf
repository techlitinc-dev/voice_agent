# Redis persistence (disaster-recovery doc §2.3).
# AOF every write (fsync every sec) + periodic RDB snapshots.

appendonly yes
appendfsync everysec

save 900 1
save 300 10
save 60 10000

# --- optional hardening extras ---
protected-mode yes
